#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../@ngx-rocket/scripts/bin/ngx-scripts" "$@"
  ret=$?
else 
  node  "$basedir/../@ngx-rocket/scripts/bin/ngx-scripts" "$@"
  ret=$?
fi
exit $ret
