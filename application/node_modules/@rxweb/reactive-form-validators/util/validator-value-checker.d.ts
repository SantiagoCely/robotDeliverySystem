import { AbstractControl } from "@angular/forms";
export declare class ValidatorValueChecker {
    static pass(control: AbstractControl, config: any): boolean;
    static passArrayValue(control: AbstractControl, config: any): boolean;
}
